services:
  mongo:
    image: mongo:7
    container_name: pharm-app-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - pharm-app-mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: pharm-app

volumes:
  pharm-app-mongo-data:
